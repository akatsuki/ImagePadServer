# v1.5 Roadmap and Code Optimization Workplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extract the v1.5 roadmap into version-independent work steps, because the local version labels have drifted from the original `v1.4.6 -> v1.5.0` roadmap.

**Architecture:** Treat each roadmap entry as a shippable workstream instead of binding it to the stale intermediate version numbers. Keep behavior fixes before refactors, then do the code-optimization splits that reduce ownership drift, and add image-format feature work after the high-risk routing and toolchain boundaries are clearer. v1.6 product features stay out of this plan, but the code split work needed to make v1.6 safer is included as an explicit optimization track.

**Tech Stack:** Go, net/http, embedded server UI, image processing, FFmpeg-backed media tooling, Windows release resources.

---

## Source Documents

- `docs/ROADMAP_v1.5.md` - original versioned roadmap.
- `docs/CODE_AUDIT_2026_06.md` - bug and code-quality source.
- `docs/GUI_AUDIT_2026_07_01.md` - GUI review notes and deferred UI candidates.
- `docs/PLAN_IMAGE_FORMAT_SETTINGS.md` - WebP and image-settings feature plan.
- `docs/PLAN_PNG_OPTIMIZE.md` - pngquant/oxipng optimization plan.

## Current Baseline

- Current code version observed in `internal/about/about.go`: `v1.4.7-dev1`.
- The intermediate roadmap labels are stale. Do not use `v1.4.8`, `v1.4.9`, etc. as the source of truth for sequencing.
- Use the workstream IDs below: `V15-00` through `V15-14`.
- Keep `docs/ROADMAP_v1.5.md` as historical input until this extracted plan replaces it.

## Scope Boundary

Included for v1.5:
- Security and permission fixes.
- Low-risk dead-code cleanup.
- History/favorites/state persistence fixes.
- Runtime quality fixes.
- Publish/Queue duplicate path reduction.
- `server.go` responsibility split.
- `publisher.go` responsibility split.
- `toolchain.go` responsibility split.
- FFmpeg/toolchain dependency boundary cleanup.
- `video` package settings dependency reduction.
- GUI audit fixes and restrained product-UI polish.
- Dark mode with explicit theme controls.
- WebP/image format preset UI.
- PNG optimization pipeline.
- v1.5 release verification.

Excluded from v1.5:
- clamp helper consolidation.
- `srgbLuminance8` cleanup.
- EXIF parser replacement.
- v1.6 music-mode UI, playlist, and party-mode work.
- Full product redesign or landing-page treatment.

---

## Phase V15-00: Baseline and Version Alignment

**Purpose:** Stop the version drift from driving the work order.

**Files:**
- Read: `internal/about/about.go`
- Read: `winres/winres.json`
- Read: `docs/ROADMAP_v1.5.md`

- [ ] Record the current app version from `internal/about/about.go`.
- [ ] Record the Windows resource version from `winres/winres.json`.
- [ ] Decide the next dev build label before any release work, for example `v1.5.0-dev1` if skipping stale intermediate tags.
- [ ] Do not change release metadata until the implementation workstreams are complete and verified.

**Verification:**
- [ ] Run: `rtk proxy pwsh -NoProfile -Command "Select-String -LiteralPath 'internal\about\about.go','winres\winres.json' -Pattern 'Version|1\.'"`
- [ ] Confirm the observed versions match the release label chosen for this plan.

---

## Phase V15-00A: Current-Code Reconciliation

**Purpose:** Some audit and roadmap items may already be fixed by recent bugfix work. Before implementing any phase, classify each item as `open`, `fixed`, `partially fixed`, or `obsolete`.

**Files:**
- Read: `docs/ROADMAP_v1.5.md`
- Read: `docs/CODE_AUDIT_2026_06.md`
- Read: current files named by each roadmap item.
- Update: this workplan or a small status ledger beside it.

- [ ] Create a status ledger with columns: `id`, `source`, `target file`, `claim`, `current evidence`, `status`, `next action`.
- [ ] Verify security items against current `internal/server/server.go`, `internal/server/auth.go`, and pairing/relay code.
- [ ] Verify dead-code claims by searching current callers before deleting anything.
- [ ] Verify storage/history claims against current `internal/library/store.go` and tests.
- [ ] Verify runtime-quality claims against current `internal/server/server.go`, especially `watchConversion`, settings reads, and `handleState`.
- [ ] Verify Publish/Queue duplication against current upload handlers after any recent upload bugfixes.
- [ ] Verify image-format and PNG plans against current `internal/imageproc` before adding new files.
- [ ] Mark any already-fixed item as `fixed` and remove it from later implementation checklists.
- [ ] Mark any changed-but-incomplete item as `partially fixed` and narrow its later implementation step to only the remaining behavior.
- [ ] Keep code-split phases even when individual bugs are fixed, because file size and ownership boundaries are separate problems.

**Initial spot-check evidence from 2026-07-01:**
- `internal/server/server.go` is still 2558 lines.
- `processSoundCloudFileAndPublish` and `processSoundCloudFileAndQueue` still exist.
- `internal/video/publisher.go` still contains `EnqueueSoundCloudForID` and `DownloadURL`.
- `internal/server/audio_ingest.go` still has `ffmpegPath2` and multiple `video.EnsureFFmpeg()` calls.
- `internal/library/store.go` still imports `io/ioutil`, uses direct `ioutil.WriteFile` for `state.json`, and discards some `addHistoryLocked` errors.
- `handleState` still has a local `recover()`.
- The settings helper methods still call `settings.Load()` directly.
- `server.go` still duplicates SoundCloud URL detection.

**Verification:**
- [ ] Run targeted `Select-String` checks before each phase and paste evidence into the ledger.
- [ ] Do not delete or refactor a function solely because the older audit said it was dead; confirm current callers first.

---

## Phase V15-01: Security and Permission Fixes

**Purpose:** Close the highest-risk behavior before touching structure.

**Files:**
- Modify: `internal/server/server.go`
- Modify or reference: `internal/server/auth.go`
- Modify or reference: `internal/server/pairing.go`
- Test: `internal/server/server_test.go`

- [ ] Add a regression test showing an `obs-relay` scoped device cannot persistently change `settings.VideoPlayerEnabled` through `/api/obs/relay-config`.
- [ ] Update `/api/obs/relay-config` handling so relay scope can start the OBS receiver but cannot perform admin-level global settings changes.
- [ ] Add or update tests for `/` and `/api/state` rejecting non-GET methods with HTTP 405.
- [ ] Add method checks to `handleIndex` and `handleState`.
- [ ] Replace `randomSuffix()` time-derived randomness with `crypto/rand`, matching the style of existing token generation.

**Verification:**
- [ ] Run focused server tests.
- [ ] Run `go test ./internal/server`.

---

## Phase V15-02: Low-Risk Dead Code and Copy/Paste Cleanup

**Purpose:** Remove misleading legacy paths before deeper routing changes.

**Files:**
- Modify: `internal/server/server.go`
- Modify: `internal/server/audio_ingest.go`
- Modify: `internal/video/publisher.go`
- Modify or reference: `internal/video/soundcloud.go`
- Test: existing server and video tests.

- [ ] Confirm `processSoundCloudFileAndPublish` has no callers.
- [ ] Confirm `processSoundCloudFileAndQueue` has no callers.
- [ ] Delete both unused SoundCloud file handlers.
- [ ] Confirm `EnqueueSoundCloudForID` has no callers.
- [ ] Delete `EnqueueSoundCloudForID`.
- [ ] Confirm `DownloadURL` has no internal callers.
- [ ] Delete `DownloadURL`.
- [ ] Rename the `ffmpegPath2` local in `processAudioFileAndQueue` to `ffmpegPath`.
- [ ] Remove the duplicate `EnsureFFmpeg()` call in `processAudioFileAndQueue`.
- [ ] Export or centralize SoundCloud URL detection so `server` does not duplicate `video` logic.
- [ ] Replace deprecated `io/ioutil` calls in `internal/library/store.go` with `os.ReadFile` and `os.WriteFile` if not already covered by Phase V15-03.

**Verification:**
- [ ] Run a repository-wide search for deleted function names.
- [ ] Run `go test ./internal/server ./internal/video ./internal/library`.

---

## Phase V15-03: Storage, History, and Favorites Stability

**Purpose:** Protect user data before refactors and feature additions.

**Files:**
- Modify: `internal/library/store.go`
- Test: `internal/library/store_test.go`

- [ ] Add a test proving `saveCurrentLocked` writes `state.json` atomically using a temporary file and rename.
- [ ] Implement atomic write for current state using the same pattern as favorites persistence.
- [ ] Add a test proving unfavorite does not implicitly remove the item from history when the history media file is gone.
- [ ] Change `SetFavorite(id, false)` so unfavorite only updates favorite state.
- [ ] Add a test proving `pruneHistoryLocked` removes the corresponding `thumb-{id}.jpg`.
- [ ] Update `pruneHistoryLocked` to remove thumbnails together with pruned media files.
- [ ] Add a test proving `SetCurrent` returns an error when `addHistoryLocked` fails.
- [ ] Stop discarding the `addHistoryLocked` error in `SetCurrent`.

**Verification:**
- [ ] Run `go test ./internal/library`.
- [ ] Run `go test ./...` after this phase because storage behavior is shared.

---

## Phase V15-04: Runtime Quality Fixes

**Purpose:** Remove long-running degradation and silent failures.

**Files:**
- Modify: `internal/server/server.go`
- Modify: `internal/server/pairing.go`
- Modify or create focused tests under `internal/server/`

- [ ] Add cancellation support to `watchConversion` so pruned jobs do not leave polling goroutines alive until the 6-hour deadline.
- [ ] Add a regression test or narrowly scoped unit test for queue prune cancellation behavior.
- [ ] Add settings caching around frequently read settings values used by `videoPlayerEnabled`, `musicModeEnabled`, `musicQualityPreset`, `videoQualityPreset`, and `obsLatencyProfile`.
- [ ] Invalidate the settings cache after settings writes that affect those values.
- [ ] Investigate the root cause behind `handleState` panic recovery.
- [ ] Fix the root cause and remove the defensive `recover()` from `handleState`.
- [ ] Replace silent `_ = settings.Update(...)` in important paths with logged error handling.

**Verification:**
- [ ] Run focused server tests.
- [ ] Run `go test ./internal/server ./internal/settings`.
- [ ] Manually call `/api/state` while toggling video player, music mode, and OBS latency settings.

---

## Phase V15-05: Publish/Queue Duplicate Path Reduction

**Purpose:** Remove the largest update-risk duplication without doing the broader v1.6 package split.

**Files:**
- Modify: `internal/server/server.go`
- Modify: `internal/server/audio_ingest.go`
- Modify: `internal/video/publisher.go`
- Test: `internal/server/upload_url_test.go`
- Test: `internal/server/server_test.go`

- [ ] Add tests covering URL publish and URL queue for the same input categories: SoundCloud, music mode, yt-dlp-supported video URL, direct downloadable media URL.
- [ ] Extract the shared `handleUploadURL` / `handleUploadURLQueue` routing into a common resolver that returns the downloaded media result and metadata.
- [ ] Keep publish vs queue as a small final action difference, not two full routing implementations.
- [ ] Merge `processVideoAndPublish` and `processVideoAndQueue` by parameterizing the source filename prefix.
- [ ] Merge `processVideoFileAndPublish` and `processVideoFileAndQueue` by passing the final store action.
- [ ] Merge `processAudioFileAndPublish` and `processAudioFileAndQueue` in the same style.
- [ ] Keep public API behavior unchanged.

**Verification:**
- [ ] Run `go test ./internal/server`.
- [ ] Run `go test ./internal/video`.
- [ ] Perform one manual publish and one manual queue action through the UI if a local server smoke test is available.

---

## Phase V15-06: Image Format Settings and WebP Support

**Purpose:** Add the v1.5 user-visible image quality feature after core correctness is stable.

**Files:**
- Modify: `internal/imageproc/processor.go`
- Create: `internal/imageproc/webp_encode.go`
- Modify: `internal/server/server.go`
- Modify: `internal/server/ui.go`
- Test: `internal/imageproc/processor_test.go`
- Test: `internal/server/server_test.go`

- [ ] Add `WebPQuality int` and `PNGQuality string` to `imageproc.Options`.
- [ ] Change `DefaultOptions()` to use WebP as the default format, with `WebPQuality` mapped to the high preset.
- [ ] Add `EncodeWebP(src, outPath, quality)` using FFmpeg via the existing local toolchain pattern.
- [ ] Flatten alpha to black before WebP encoding, matching the existing JPEG flattening behavior.
- [ ] Add `"webp"` to the image format switch in `Process`.
- [ ] Add server-side preset maps for JPEG, WebP, and PNG quality names.
- [ ] Update `optionsFromValues` to accept both new preset names and old numeric quality values for compatibility.
- [ ] Replace image option numeric fields in the UI with select-based presets.
- [ ] Add JavaScript quality-option switching for PNG vs WebP/JPEG.
- [ ] Add `updateUploadControlsVisibility()` so upload controls hide when `obsMode || state.videoPlayerEnabled`.

**Verification:**
- [ ] Add and run WebP processor tests.
- [ ] Add and run preset parsing tests.
- [ ] Manually upload an image and confirm WebP output has `image/webp`.
- [ ] Manually toggle OBS mode and video player mode and confirm image-only controls hide correctly.

---

## Phase V15-07: PNG Optimization Pipeline

**Purpose:** Reduce PNG transfer size while keeping failures best-effort.

**Files:**
- Modify: `internal/imageproc/processor.go`
- Create: `internal/imageproc/tools.go`
- Create: `internal/imageproc/png_optimize.go`
- Modify: application startup path that currently invokes video tool validation.
- Test: `internal/imageproc/processor_test.go`
- Test: new `internal/imageproc/png_optimize_test.go`

- [ ] Add `EnsurePngquant()` with environment override, bundled path lookup, PATH lookup, and Windows download support.
- [ ] Add `EnsureOxipng()` with environment override, bundled path lookup, PATH lookup, and Windows/macOS download support.
- [ ] Add checksum override variables: `IMAGEPAD_PNGQUANT_SHA256` and `IMAGEPAD_OXIPNG_SHA256`.
- [ ] Add `OptimizePNG(path, quality string)` with `lossless` using oxipng only.
- [ ] For non-lossless PNG presets, run pngquant with the preset quality range, then oxipng.
- [ ] Treat missing tools and optimization failures as best-effort logs, not upload failures.
- [ ] Change PNG encoding to `png.BestCompression`.
- [ ] Call `OptimizePNG` after PNG output is written.
- [ ] Add `ValidateImageTools()` and call it during startup alongside existing media tool validation.

**Verification:**
- [ ] Run imageproc tests with fake or environment-provided tool paths.
- [ ] Run one real PNG optimization smoke test on Windows if tool download is allowed.
- [ ] Confirm PNG uploads still work when pngquant and oxipng are unavailable.

---

## Phase V15-08: GUI Audit Fixes and Dark Mode

**Purpose:** Fold the GUI audit recommendations into a focused product-UI pass, and add a real dark mode without turning the app into a full redesign.

**Files:**
- Read: `docs/GUI_AUDIT_2026_07_01.md`
- Modify: `internal/server/ui.go`
- Modify or create tests only if existing server/UI snapshot tests cover emitted HTML.
- Optional doc update: `docs/ROADMAP.md` or release notes.

- [ ] Keep the design register as product UI: restrained, task-focused, dense but readable, no decorative redesign.
- [ ] Convert hard-coded light colors in `internal/server/ui.go` to semantic CSS variables for background, surface, surface-subtle, text, muted text, border, accent, danger, warning, success, focus ring, and overlay.
- [ ] Add light theme tokens under `:root`.
- [ ] Add dark theme tokens under `html[data-theme="dark"]`.
- [ ] Set `color-scheme: light dark` and ensure form controls render correctly in both themes.
- [ ] Add a compact theme toggle in the header or top status area with options `自動`, `ライト`, `ダーク`.
- [ ] Store the manual theme choice in `localStorage`; use `prefers-color-scheme` only when the choice is `auto` or absent.
- [ ] Apply the theme before first paint with a small inline script before the main CSS or at the start of `<head>` to avoid a light flash.
- [ ] Add focus-visible styles for buttons, tabs, inputs, selects, switches, and icon buttons with contrast-safe focus rings in both themes.
- [ ] Verify body text and muted text meet WCAG contrast in both themes.
- [ ] Update the tool install overlay copy from the GUI audit: show `再試行中` while automatic retries are active, and show a concise manual recovery hint only after retries are exhausted.
- [ ] Normalize pairing panel language so Japanese is primary, retaining English only for product names or protocol terms.
- [ ] Revisit the queue tab label `動画変換`; if audio visualizer jobs appear there, rename to a broader Japanese label such as `変換キュー` after confirming it does not confuse existing users.
- [ ] Reduce quit button visual dominance: keep it accessible, but separate it from normal workflow controls through spacing, secondary danger styling, and clearer title/copy.
- [ ] Add visible compact percentage text beside or inside long-running progress bars for conversions and tool installs.
- [ ] Add `aria-live="polite"` or equivalent status text for progress and tool-install state changes without making the UI noisy.
- [ ] Ensure theme changes do not affect QR readability, OBS protected overlay readability, drag-drop overlay readability, or stream-key reveal controls.
- [ ] Ensure mobile layout still fits controls without text overflow at the existing breakpoints.

**Verification:**
- [ ] Run `go test ./internal/server` after editing embedded UI.
- [ ] Launch the app locally and verify `/` in light, dark, and auto modes.
- [ ] Use a browser visual check for desktop width, tablet/mobile width, and short-height desktop.
- [ ] Check the main flows: image upload, URL upload, OBS tab, history/favorites/queue tabs, tool install overlay, progress bars, quit action.
- [ ] Check Windows high-contrast or forced-colors behavior if available; otherwise confirm no critical state relies on color alone.

---

## Phase V15-09: Server Responsibility Split

**Purpose:** Split the most bloated file, `internal/server/server.go`, into focused server files while preserving the `Server` type and public route behavior.

**Files:**
- Modify: `internal/server/server.go`
- Create: `internal/server/routes.go`
- Create: `internal/server/upload.go`
- Create: `internal/server/upload_url.go`
- Create: `internal/server/history.go`
- Create: `internal/server/obs.go`
- Create: `internal/server/media_serve.go`
- Create: `internal/server/state.go`
- Create: `internal/server/settings_handlers.go`
- Create: `internal/server/share_urls.go`
- Create: `internal/server/json.go`
- Test: existing `internal/server` tests.

- [ ] Treat this as a mechanical split after Phase V15-05 has reduced Publish/Queue duplication.
- [ ] Keep `Server` struct, `New`, and setter methods in `server.go`.
- [ ] Move `Register` and route wiring to `routes.go`.
- [ ] Move file upload, image processing entry points, and shared upload helpers to `upload.go`.
- [ ] Move URL upload handlers and URL acquisition routing to `upload_url.go`.
- [ ] Move history list, favorite, queue, select, and history media handlers to `history.go`.
- [ ] Move OBS stream callbacks, OBS start/end/key/latency/relay-config handlers, and OBS state helpers to `obs.go`.
- [ ] Move `/image`, `/video`, `/stream`, generated-file serving, HLS segment serving, favicon, and health handlers to `media_serve.go`.
- [ ] Move `state`, `stateWithMedia`, `historyState`, `videoQueueState`, `videoPlayerState`, quality-state, and OBS-state composition to `state.go`.
- [ ] Move video player, music mode, FFmpeg install, video quality, and network check handlers to `settings_handlers.go`.
- [ ] Move admin URL, clipboard target, primary share URL, and copy-target helpers to `share_urls.go`.
- [ ] Move `writeJSON` and small response helpers to `json.go`.
- [ ] Keep `safeFileName`, `randomSuffix`, and small package-level helpers near their callers after the split.
- [ ] Do not change route paths, request/response schemas, auth wrappers, or status codes in this phase except where earlier phases already required behavior fixes.
- [ ] Run `gofmt` on all touched Go files.

**Verification:**
- [ ] Run `go test ./internal/server`.
- [ ] Run `go test ./...` because route registration and state composition are central.
- [ ] Run `rtk proxy pwsh -NoProfile -Command "(Get-Content -LiteralPath 'internal\server\server.go').Count"` and confirm `server.go` is reduced to the core type/setup file.
- [ ] Run a local UI smoke test and confirm `/`, `/api/state`, upload, history, OBS settings, and current media serving still respond.

---

## Phase V15-10: Publisher Responsibility Split

**Purpose:** Split `internal/video/publisher.go` into focused files without changing public behavior.

**Files:**
- Modify: `internal/video/publisher.go`
- Create: `internal/video/queue.go`
- Create: `internal/video/conversion.go`
- Create: `internal/video/download.go`
- Create: `internal/video/quality.go`
- Create: `internal/video/thumbnails.go`
- Test: existing `internal/video` tests.

- [ ] Inventory exported types and functions in `internal/video/publisher.go`.
- [ ] Keep exported API names stable unless all callers are updated in the same phase.
- [ ] Move queue state, queue item types, enqueue/dequeue helpers, and queue status transitions to `queue.go`.
- [ ] Move FFmpeg conversion orchestration, HLS/MP4 publishing helpers, and still-image publishing internals to `conversion.go`.
- [ ] Move URL download and yt-dlp mediated source acquisition helpers to `download.go`.
- [ ] Move quality preset calculation, bitrate selection, and network-derived quality helpers to `quality.go`.
- [ ] Move thumbnail generation and thumbnail path helpers to `thumbnails.go`.
- [ ] Leave small compatibility wrappers in `publisher.go` only when they reduce churn for server callers.
- [ ] Run `gofmt` on all touched Go files.

**Verification:**
- [ ] Run `go test ./internal/video`.
- [ ] Run `go test ./internal/server` because server routes call video package APIs heavily.
- [ ] Run a focused search to confirm no duplicate moved functions remain in `publisher.go`.

---

## Phase V15-11: Toolchain Responsibility Split

**Purpose:** Split `internal/video/toolchain.go` so path resolution, downloads, archive handling, checksums, and command execution are independently maintainable.

**Files:**
- Modify: `internal/video/toolchain.go`
- Create: `internal/video/toolchain_paths.go`
- Create: `internal/video/toolchain_download.go`
- Create: `internal/video/toolchain_archive.go`
- Create: `internal/video/toolchain_verify.go`
- Create: `internal/video/exec.go`
- Test: existing video toolchain tests, if present.

- [ ] Inventory all functions in `toolchain.go` and group them by responsibility before moving code.
- [ ] Move environment-variable and bundled-path lookup helpers to `toolchain_paths.go`.
- [ ] Move FFmpeg, FFprobe, yt-dlp, and related download functions to `toolchain_download.go`.
- [ ] Move zip/tar extraction helpers to `toolchain_archive.go`.
- [ ] Move SHA256 and downloaded-binary validation helpers to `toolchain_verify.go`.
- [ ] Move `run`, `runInDir`, and `runInDirContext` to `exec.go`.
- [ ] Keep exported toolchain entry points such as `EnsureFFmpeg`, `EnsureYTDLP`, and `ValidateInstalledTools` stable.
- [ ] Remove duplicated reads of `IMAGEPAD_FFPROBE`, `IMAGEPAD_YTDLP`, `IMAGEPAD_FFMPEG_SHA256`, and `IMAGEPAD_YTDLP_SHA256` where the split exposes a single helper.
- [ ] Run `gofmt` on all touched Go files.

**Verification:**
- [ ] Run `go test ./internal/video`.
- [ ] Run any local tool-install tests only if the environment variables for real network/tool tests are intentionally enabled.
- [ ] Confirm missing-tool behavior still uses the established `%APPDATA%\ImagePadServer\bin` pattern on Windows.

---

## Phase V15-12: Toolchain Package Boundary

**Purpose:** Stop `imageproc` and `obsrtmp` from depending on `video` solely to find FFmpeg.

**Files:**
- Create: `internal/toolchain/`
- Move or wrap: FFmpeg/FFprobe/yt-dlp path resolution from `internal/video`
- Modify: `internal/imageproc/processor.go`
- Modify: `internal/imageproc/webp_encode.go`
- Modify: `internal/obsrtmp/manager.go`
- Modify: `internal/server/` files that call `video.EnsureFFmpeg`
- Modify: `internal/video/` callers to use the new package internally.

- [ ] Create `internal/toolchain` with stable entry points for FFmpeg, FFprobe, yt-dlp, command execution, and installed-tool validation.
- [ ] Move implementation from the split video toolchain files into `internal/toolchain` where it is not video-specific.
- [ ] Keep temporary wrappers in `internal/video` for `EnsureFFmpeg`, `EnsureFFprobe`, `EnsureYTDLP`, and `ValidateInstalledTools` if that keeps this phase small.
- [ ] Update `imageproc` to call `toolchain.EnsureFFmpeg` instead of importing `video`.
- [ ] Update `obsrtmp` to call `toolchain.EnsureFFmpeg` instead of importing `video`.
- [ ] Update server code to use `toolchain` directly where the operation is not video-domain behavior.
- [ ] Delete temporary video wrappers only after all external callers are moved.
- [ ] Ensure this phase does not change download URLs, checksum policy, or install locations.

**Verification:**
- [ ] Run `go test ./internal/toolchain ./internal/video ./internal/imageproc ./internal/obsrtmp ./internal/server`.
- [ ] Run a build to catch import-cycle regressions.
- [ ] Confirm `go list ./...` succeeds with no import cycles.

---

## Phase V15-13: Settings Dependency Reduction

**Purpose:** Reduce direct dependency from lower media packages to global settings, especially after toolchain code moves out of `video`.

**Files:**
- Modify: `internal/video/process_registry.go`
- Modify: any remaining `internal/video` files importing `internal/settings`
- Modify: `internal/app/app.go`
- Modify: server construction or publisher setup code as needed.

- [ ] Search for `internal/settings` imports under `internal/video`.
- [ ] For each remaining use, decide whether it is a data-directory need, a persistent preference need, or a process-registry need.
- [ ] Pass the data directory explicitly from app/server setup into video package functions that need file locations.
- [ ] Introduce a small config struct or interface only if more than two call sites need the same settings values.
- [ ] Keep settings writes in `internal/settings` and server/app-level code, not in lower media helpers.
- [ ] Confirm `internal/video` no longer imports `internal/settings` after this phase.

**Verification:**
- [ ] Run `rtk proxy pwsh -NoProfile -Command "Select-String -Path 'internal\video\*.go' -Pattern 'internal/settings'"`
- [ ] Run `go test ./internal/video ./internal/app ./internal/server`.
- [ ] Run `go test ./...` if constructor signatures changed across packages.

---

## Phase V15-14: Release Gate

**Purpose:** Cut a coherent v1.5 release only after all included workstreams are verified.

**Files:**
- Modify: `internal/about/about.go`
- Modify: `winres/winres.json`
- Regenerate: `cmd/imagepadserver/rsrc_windows_amd64.syso` if Windows resources changed.
- Reference: release scripts and workflows.

- [ ] Run `go test ./...`.
- [ ] Manually verify WebP output in expected consumers.
- [ ] Manually verify PNG optimization ratios for lossless and lossy presets.
- [ ] Manually verify video player mode ON/OFF UI behavior.
- [ ] Manually verify favorite add, unfavorite, and reselect behavior.
- [ ] Manually verify state persistence survives an interrupted write scenario where practical.
- [ ] Update `internal/about/about.go` to the chosen v1.5 release or dev label.
- [ ] Update `winres/winres.json` to the matching file/product version.
- [ ] Regenerate Windows resources.
- [ ] Build the Windows artifact using the repo's established release path or bundled Go fallback.
- [ ] Commit as dev build first if needed, then cut `release: v1.5.0` only after smoke verification.

**Verification:**
- [ ] Confirm `internal/about/about.go`, `winres/winres.json`, artifact name, and release notes agree on one version.
- [ ] Confirm the produced binary launches and serves the management UI.
- [ ] Confirm no stale intermediate version labels remain in release metadata.

---

## Deferred Code Optimization Backlog

These were identified in the source roadmap and audit but remain lower-risk cleanup after the explicit split track above.

- Consolidate clamp helpers after the active rendering code settles.
- Remove `srgbLuminance8` duplication after visualizer tests cover output stability.
- Replace or harden the handwritten EXIF parser if image-orientation bugs appear in real input.

## Suggested Commit Slices

- `fix: tighten relay scope and HTTP methods`
- `refactor: remove obsolete SoundCloud paths`
- `fix: harden media store persistence`
- `fix: reduce settings I/O and conversion watchers`
- `refactor: share upload publish and queue routing`
- `style: add dark mode and gui audit fixes`
- `refactor: split server responsibilities`
- `refactor: split video publisher responsibilities`
- `refactor: split media toolchain responsibilities`
- `refactor: move toolchain out of video package`
- `refactor: remove video settings dependency`
- `feat: add image format presets and WebP output`
- `feat: optimize PNG output`
- `release: v1.5.0`
