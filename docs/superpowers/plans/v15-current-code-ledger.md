# v1.5 Current-Code Reconciliation Ledger

Date: 2026-07-01
Worktree: `.worktrees/v15-roadmap-execution`
Branch: `codex/v15-roadmap-execution`

This ledger classifies roadmap/audit items against the current worktree before implementation. Status values: `open`, `fixed`, `partially fixed`, `obsolete`.

| id | source | target file | claim | current evidence | status | next action |
|---|---|---|---|---|---|---|
| V15-00 | plan | `internal/about/about.go`, `winres/winres.json` | Version labels drifted from roadmap. | Chosen dev label is `v1.5.0-dev1` with file/product version `1.5.0.1`; `about.go`, `winres.json`, generated resource, and artifact name are aligned. | fixed | Stable `v1.5.0` tag/release still requires explicit user release request. |
| V15-01A | audit | `internal/server/server.go` | `handleIndex` and `handleState` need HTTP method checks. | Added `TestIndexAndStateRejectNonGET`; initial red returned 200 for both; implementation now returns 405. | fixed | Covered by `go test ./internal/server -run TestIndexAndStateRejectNonGET -count=1`. |
| V15-01B | audit | `internal/server/server.go` | `randomSuffix()` uses predictable time-derived value. | Replaced time-only suffix with `crypto/rand` bytes encoded as hex, with time fallback only on entropy failure. | fixed | Covered by compile and server tests. |
| V15-01C | audit | `internal/server/server.go` | Relay-scoped OBS device must not persist global video-player setting. | Added relay-device regression test; admin path persists setting, relay-device path starts receiver without settings write. | fixed | Covered by `go test ./internal/server -run 'TestOBSRelayConfig|TestIndexAndStateRejectNonGET' -count=1`. |
| V15-02A | audit | `internal/server/server.go` | Obsolete SoundCloud publish/queue functions remain. | Exact search for `processSoundCloudFileAndPublish` and `processSoundCloudFileAndQueue` returns no production hits. | fixed | No action. |
| V15-02B | audit | `internal/video/publisher.go` | `EnqueueSoundCloudForID` remains. | Caller search found definition only; removed. | fixed | Search for `EnqueueSoundCloudForID` returns no production code hits. |
| V15-02C | audit | `internal/video/publisher.go` | `DownloadURL` compatibility wrapper remains. | Caller search found definition only; removed. | fixed | Search for `DownloadURL(` returns only unrelated Darwin toolchain symbols. |
| V15-02D | audit | `internal/server/audio_ingest.go` | `ffmpegPath2` and duplicate `EnsureFFmpeg()` remain. | Removed duplicate calls and renamed queue path local to `ffmpegPath`. | fixed | Search for `ffmpegPath2` returns no hits. |
| V15-02E | audit | `internal/library/store.go` | `io/ioutil` deprecated. | `Select-String` did not find `ioutil`; import appears removed in this worktree. | fixed | No action unless full file inspection contradicts. |
| V15-02F | audit | `internal/server/server.go`, `internal/video/soundcloud.go` | SoundCloud URL detection duplicated in server. | Added exported `video.IsSoundCloudURL` wrapper and changed server URL upload paths to use it; removed server-local duplicate. | fixed | Covered by `go test ./internal/server ./internal/video`. |
| V15-03A | audit | `internal/library/store.go` | `SetCurrent` discards `addHistoryLocked` errors. | Added `TestSetCurrentReturnsHistoryCopyError`; `SetCurrent` now returns history copy errors before setting current. | fixed | Covered by `go test ./internal/library -count=1`. |
| V15-03B | audit | `internal/library/store.go` | `state.json` write should be atomic. | `saveCurrentLocked` now writes `state.json.tmp` then renames to `state.json`, matching favorites pattern. | fixed | Covered by `go test ./internal/library -count=1`; direct crash simulation still pending for release gate. |
| V15-03C | audit | `internal/library/store.go` | `pruneHistoryLocked` should remove thumbnails. | Added `TestPruneHistoryRemovesThumbnail`; pruned history thumbnails are now removed. | fixed | Covered by `go test ./internal/library -count=1`. |
| V15-03D | audit | `internal/library/store.go` | Unfavorite should not implicitly remove history. | Added `TestUnfavoriteDoesNotRemoveHistoryWhenWorkingFileWasPruned`; unfavorite now clears favorite flags without removing history item. | fixed | Covered by `go test ./internal/library -count=1`. |
| V15-04A | audit | `internal/server/server.go` | `handleState` has defensive recover. | Removed the local `recover()` from `handleState`; `/api/state` uses a single settings snapshot and server tests cover state response paths. | fixed | Covered by focused server tests and `go test ./internal/server ./internal/settings`. |
| V15-04B | audit | `internal/server/server.go` | Settings helper methods reread disk. | Added per-`Server` settings cache via `appSettings()`, invalidated through `updateSettings()` after server-side settings writes. `settings.Load()` remains only in `loadSettingsOrDefault()`. | fixed | Covered by focused server/settings tests and full `go test ./...`. |
| V15-04C | audit | `internal/app`, `internal/server` | Important `settings.Update` calls should not fail silently. | Replaced remaining `_ = settings.Update` sites with logged error handling in app network measurement, relay last-seen, and tool install commit/revert paths. | fixed | Search for `_ = settings.Update` returns no hits; covered by `go test ./internal/server ./internal/app`. |
| V15-04D | audit | `internal/server/server.go` | `watchConversion` can poll missing/pruned queue jobs until timeout. | `watchConversion` now looks up the queue item by job ID on each tick and returns when the job is gone; added `TestQueueItemByID`. | fixed | Covered by `go test ./internal/server -run 'TestQueueItemByID|TestIndexAndStateRejectNonGET|TestOBSRelayConfig' -count=1 -v`. |
| V15-05 | audit | `internal/server/server.go` | Publish/Queue upload URL routing is duplicated. | Added `handleUploadURLAction` / `handleVideoURLAction`, `uploadURLAction`, shared audio/video file processors, and publish/queue routing tests for SoundCloud, music mode, yt-dlp page URLs, and direct media fallback. | fixed | Covered by `go test ./internal/server`, `go test ./internal/video`, and `go test ./internal/server ./internal/video`. |
| V15-06 | feature plan | `internal/imageproc`, `internal/server/ui.go` | WebP/image preset UI not implemented. | Added WebP default options, FFmpeg-backed WebP encode, image quality presets, select-based UI controls, and controls hiding for OBS/video-player modes. | fixed | Covered by `go test ./internal/imageproc ./internal/server`; browser/manual upload smoke still pending for release gate. |
| V15-07 | feature plan | `internal/imageproc` | PNG optimization pipeline not implemented. | Added `tools.go`, `png_optimize.go`, BestCompression PNG output, `OptimizePNG` hook, startup `ValidateImageTools`, and no-tools regression coverage. | fixed | Covered by `go test ./internal/imageproc ./internal/server ./internal/app`; real tool download smoke still pending for release gate. |
| V15-08 | GUI audit | `internal/server/ui.go` | GUI audit fixes and dark mode needed. | Added light/dark tokens, first-paint theme script, header theme selector, localStorage/auto handling, focus-visible rings, Japanese pairing copy, `変換キュー` labels, retry/exhausted install copy, and progress percentages. | fixed | Covered by `go test ./internal/server`; browser visual QA remains for release gate. |
| V15-09 | audit | `internal/server/server.go` | `server.go` is bloated. | Split routes, JSON, settings handlers, share URLs, state composition, OBS handlers, history/queue handlers, upload handlers, and media serving into focused files; `server.go` is now 403 lines. | fixed | Covered by repeated `go test ./internal/server`; full `go test ./...` remains release-gate coverage. |
| V15-10 | audit | `internal/video/publisher.go` | `publisher.go` is bloated. | Split quality, network measurement, generated-file/HLS helpers, queue execution, media acquisition/thumbnailing, and status composition into focused files; `publisher.go` is now 236 lines. | fixed | Covered by `go test ./internal/video` and `go test ./internal/server`. |
| V15-11 | audit | `internal/video/toolchain.go` | `toolchain.go` is bloated. | Split command execution, downloads/checksums/validation, archive/migration, and cleanup helpers into focused files; `toolchain.go` is now 384 lines. | fixed | Covered by `go test ./internal/video`. |
| V15-12 | audit | `internal/video`, `internal/imageproc`, `internal/obsrtmp` | Toolchain logic should not live in video package. | Moved toolchain implementation, process tracking, hide-window helpers, install progress, tool-source selection, and toolchain tests to `internal/toolchain`; `internal/video` now keeps compatibility wrappers only. | fixed | Covered by `go test ./internal/toolchain ./internal/video ./internal/imageproc ./internal/obsrtmp ./internal/server` after rerun; full verification pending below. |
| V15-13 | audit | `internal/video` | Video package imports settings directly. | `Select-String` for `internal/settings` under `internal/video/*.go` returns no hits after moving data-dir/tool-install concerns to `internal/toolchain`. | fixed | Covered by search plus package tests; full `go test ./...` pending below. |
